import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App should build without error', (WidgetTester tester) async {
    // Placeholder test — akan diganti saat fitur pertama dibuat
    expect(true, isTrue);
  });
}
